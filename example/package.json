{
  "name": "example",
  "version": "1.1.0",
  "description": "Example of using micdrop.",
  "main": "server.js",
  "scripts": {
    "test": "mocha --reporter min --recursive --watch",
    "test:debug": "npm test -- debug",
    "test:cover": "istanbul cover node_modules/.bin/_mocha -- -u exports -R spec test/**/* --report html"
  },
  "author": "Kent Primrose",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.3",
    "micdrop": "^0.1.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "chai-http": "^3.0.0",
    "istanbul": "^0.4.5",
    "memrest": "^0.1.0",
    "mocha": "^3.2.0"
  }
}
